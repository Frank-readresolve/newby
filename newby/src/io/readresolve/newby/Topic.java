package io.readresolve.newby;

/**
 * Represents a topic.
 *
 * @author Frank MARSHALL
 */
public enum Topic {
    /** Java SE topic. */
    JAVA_SE,
    /** Java EE topic. */
    JAVA_EE,
    /** Git topic. */
    GIT,
    /** Jira topic. */
    JIRA,
    /** Spring core topic. */
    SPRING_CORE,
    /** Spring security topic. */
    SPRING_SECURITY;
}
